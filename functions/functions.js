const {
  simpleAsyncReturn,
  simpleSyncReturn,
} = require('./finished/await-speed')
const {
  returnAwait,
  simpleReturn,
  simpleAwait,
} = require('./finished/return-await')

module.exports = {
  simpleAsyncReturn,
  simpleSyncReturn,

  returnAwait,
  simpleReturn,
  simpleAwait,
}
